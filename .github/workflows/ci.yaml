name: Pain Engine CI
permissions:
  contents: read

on:
  push:
    branches:
      - main
      - more-cicd
  pull_request:
    branches: 
      - main

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  CMAKE_BUILD_PARALLEL_LEVEL: 8

jobs:
  cppcheck:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v5
    
    - name: Cache Cppcheck
      id: cache-cppcheck
      uses: actions/cache@v4
      with:
        path: "C:\\Program Files\\Cppcheck"
        key: cppcheck-2.18.0

    - name: Download and Install Cppcheck
      if: steps.cache-cppcheck.outputs.cache-hit != 'true'
      run: |
        Invoke-WebRequest -Uri "https://github.com/danmar/cppcheck/releases/download/2.18.0/cppcheck-2.18.0-x64-Setup.msi" -OutFile "cppcheck-setup.msi"
        Start-Process msiexec.exe -Wait -ArgumentList '/I cppcheck-setup.msi /quiet'

    - name: Run cppcheck
      run: |
        & "C:\Program Files\Cppcheck\cppcheck.exe" `
          --error-exitcode=1 `
          --force `
          --enable=warning,style `
          --inconclusive `
          --std=c++20 `
          --suppress=missingIncludeSystem `
          --suppress=missingInclude `
          -i vendor `
          -D PN_PLATFORM_WINDOWS `
          -D PN_PLATFORM_ANDROID `
          --check-level=exhaustive `
          --output-file=cppcheck_report.txt .
          
    - name: Display Cppcheck report on failure
      if: failure()
      run: |
        if (Test-Path cppcheck_report.txt) {
          Get-Content cppcheck_report.txt
        }
        
    - name: Upload Cppcheck report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: cppcheck_report
        path: cppcheck_report.txt

  build-windows:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        configuration: [Debug, Release]
    steps:
      - uses: actions/checkout@v5

      - name: Cache CMake _deps
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: ${{ runner.os }}-windows-deps-${{ hashFiles('**/CMakeLists.txt', 'cmake/*.cmake') }}
          restore-keys: |
            ${{ runner.os }}-windows-deps-

      - name: Setup MSBuild.exe
        uses: microsoft/setup-msbuild@v2

      - name: Configure and Build (build.bat)
        run: |
          .\build.bat
          cmake --build build --config ${{ matrix.configuration }} -j 8

      - name: Setup Software OpenGL (Mesa)
        run: |
          # Download Mesa for Windows (software rendering for OpenGL 4.5 support)
          Invoke-WebRequest -Uri "https://github.com/pal1000/mesa-dist-win/releases/download/24.0.3/mesa3d-24.0.3-release-msvc.7z" -OutFile "mesa.7z"
          
          # Extract the opengl32.dll (Requires 7zip, which is on windows-latest)
          7z e mesa.7z x64/opengl32.dll -y
          
          # Copy to BOTH bin/Debug and bin/Release
          # (One will fail depending on matrix, so we use -ErrorAction SilentlyContinue)
          Copy-Item "opengl32.dll" "bin/${{ matrix.configuration }}/" -ErrorAction SilentlyContinue
        shell: pwsh

      - name: Run executable and test for graceful exit
        run: |
          cd bin/${{ matrix.configuration }}
          
          # Start the process
          $proc = Start-Process .\Friends-Frog-ever.exe -PassThru
          Write-Host "Process started with PID: $($proc.Id)"
          
          # Wait for it to initialize
          Start-Sleep -Seconds 10
          
          # DIAGNOSTICS: Check if still running
          if ($proc.HasExited) {
             Write-Error "Application exited unexpectedly with code $($proc.ExitCode) before test started."
             exit 1
          }

          # DIAGNOSTICS: Check for Window Handle
          Write-Host "MainWindowHandle: $($proc.MainWindowHandle)"
          
          # Attempt graceful close
          Write-Host "Attempting CloseMainWindow()..."
          $success = $proc.CloseMainWindow()
          Write-Host "CloseMainWindow returned: $success"

          if (!$success) { 
             Write-Host "Falling back to taskkill (graceful)..."
             taskkill /IM Friends-Frog-ever.exe 
          }

          # Wait for exit
          $proc.WaitForExit(5000)
          if (!$proc.HasExited) {
              Write-Error "${{ matrix.configuration }} build failed to exit gracefully."
              Stop-Process -Id $proc.Id -Force
              exit 1
          }
          Write-Host "Application exited gracefully."
        shell: pwsh

  build-android:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Install NDK 27.0.12077973
        run: |
          $sdkmanager = "$env:ANDROID_HOME\cmdline-tools\latest\bin\sdkmanager"
          & $sdkmanager "ndk;27.0.12077973"

      - name: Setup MSBuild.exe
        uses: microsoft/setup-msbuild@v2

      - name: Configure cross-platform files
        run: .\build.bat

      - name: Build Android app
        working-directory: android
        run: .\gradlew.bat assembleDebug assembleRelease --stacktrace --parallel
        env:
          SKIP_SIGNING: true 
          CI_ABI: x86_64

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: android/app/build/outputs/apk/release/*.apk  
