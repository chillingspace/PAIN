# Build all tools (only on host platforms)
if(NOT ANDROID)
    message(STATUS "Building PAIN Tools for asset pipeline")
    add_subdirectory(AssetCompiler)
else()
    message(STATUS "Skipping Tools for Android target")
endif()